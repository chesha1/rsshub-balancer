# TODO

## 使用 LogTape 内置日志格式化

- [ ] 使用 `getJsonLinesFormatter()` 替换手写日志格式化和错误序列化。

当前日志层自行拼接 message、定义 JSON 结构，并序列化 `Error`、`cause` 和 `AggregateError`；已安装的 LogTape 已提供这些格式化能力。

后续采用 LogTape 内置日志格式化，将原始错误交由库处理，删除手写 message 拼接、JSON 结构和 `errorProps` 转换，不增加依赖。

实施时确认日志字段布局变化，例如 `timestamp` 变为 `@timestamp`、`category` 变为 `logger`、错误字段改为嵌套对象。验证正常错误链，以及循环引用等异常值的兜底边界；必要时仅保留简短兜底，不继续维护完整递归序列化器。保留当前警告级别、请求上下文和运行时警告诊断。本次仅记录待办，尚未实施。

## 缓存探测后随机选择命中实例

- [ ] 缓存探测后，从确认命中的实例集合中等概率随机选择上游，改善请求分配的公平性。

当前 `upstream.ts` 使用 `Promise.any` 选择最先返回 200 的缓存命中实例；探测前打乱候选顺序仍会让响应更快的实例反复胜出。

后续保留缓存优先，在有界的并发探测阶段收集返回 200 的实例，再从命中集合中随机选择首个请求目标。每次请求重新选择，不通过最近成功提示将路径固定到某个实例；无缓存命中时继续随机尝试可用候选，保留失败标记过滤和失败重试。

实施时明确探测结果收集的等待上限，评估等待更多结果带来的额外延迟；验证多个命中实例均有首选机会，以及单命中、无命中、探测超时和请求失败后的行为。此项改善缓存命中实例之间的分配，整体流量仍受各实例缓存覆盖范围影响。

## 首页图表异步加载

- [ ] 使用 Vue 异步组件加载桑基图，让 Vite 自动分包，并评估移除手写 vendor 分包规则。

当前 `App.vue` 静态导入 `TrafficSankeyChart.vue`，ECharts 虽被拆成独立文件，仍随页面启动预加载；首页正文展示与 `onMounted` 中的数据请求也需等待这些启动依赖就绪。

后续使用 `defineAsyncComponent()` 配合动态 `import()` 划分图表加载边界，优先让正文显示、语言按钮可用和数据请求发起。沿用当前条件渲染时，统计为空或请求失败可跳过图表代码加载；正常显示图表仍需下载相关代码，总下载量通常不会明显减少。

取舍：若等统计数据成功返回后才加载图表代码，会增加后续等待阶段，图表首次显示不一定更快；若更重视图表显示速度，可评估并行请求统计数据和加载图表代码，但会失去空数据或接口失败时省去下载的收益。此前临时副本构建记录的启动 JS gzip 约 245 KB → 91 KB 仅是构建体积对比，不能据此推断实际加载时间改善幅度。

实施时检查构建产物，确认图表专用依赖不再随入口预加载；对比首次访问和命中浏览器缓存时的正文可见时间、接口发起时间与图表首次显示时间，并覆盖代码加载中、加载失败、统计为空及接口失败的展示状态，再确定加载时机。

## 丰富桑基图字段

- [ ] 评估更有展示价值的字段和图表组合。

候选方向：

- HTTP 响应结果、处理耗时档位；当前不采集，若需要展示须重新加入指标。
- Cloudflare 提供的来源网络 ASN、省区或城市等信息。
- 根据 RSSHub 路由映射的内容来源类别，仅记录分类。
- 根据 `User-Agent` 得到的阅读器、浏览器、脚本等客户端粗分类。

后续按信息量、可读性和新增采集成本选择字段，再同步指标协议、聚合查询与前端。当前只记录国家与上游，展示 `country -> upstream`，保留维度复选框和按可见列绘图的逻辑。

## 不再使用的 HTTP 响应体释放

- [ ] 统一消费或取消已经取得但不再使用的 HTTP 响应体。

当前 Workers 代码已有只读取状态码后丢弃 `Response` 的路径，属于现有代码改进，单独安排，不作为本次云上云下迁移的实施或验收要求。

处理范围：

- 上游请求：缓存探测的成功和失败响应、失败后继续尝试其他上游的响应，以及远程实例列表抓取失败的响应。
- 定时健康检查和应用接口：只读取状态码的健康检查，以及 `/api/route/status` 中失败或未被 `Promise.any` 选中的已返回响应。
- 指标查询：查询失败后不再使用的错误响应。

保留当前探测范围、选路顺序和超时策略，不要求提前取消仍在进行的其他探测；正常转发给客户端的响应体继续交由转发链路使用。后续修复优先使用两端可复用的标准 API。

Cloudflare 建议取消不需要的响应体以释放内存；Node/Undici 明确要求消费或取消响应体，避免依赖垃圾回收影响连接复用和连接资源释放。目前确认的是代码缺口，尚无线上资源泄漏或故障的实测结论。

依据：[Cloudflare 响应体取消建议](https://developers.cloudflare.com/workers/platform/limits/#simultaneous-open-connections)、[Undici Garbage Collection](https://github.com/nodejs/undici#garbage-collection)。
