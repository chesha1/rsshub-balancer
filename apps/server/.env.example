# 复制为 apps/server/.env 后填写；真实配置不提交。
VALKEY_URL=redis://127.0.0.1:6379
PORT=3000

# 同时配置这两个值后启用业务查询；凭据仅保留在服务端。
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_ANALYTICS_API_TOKEN=

# 配置 URL 后启用上传；Cloudflare WAF 限制服务器公网出口 IP。
# 未配置只适合本地 smoke，正式切流必须启用。
METRICS_INGEST_URL=
